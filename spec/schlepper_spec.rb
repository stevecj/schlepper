require 'spec_helper'

describe Schlepper do
  it 'has a version number' do
    expect(Schlepper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
